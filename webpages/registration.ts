
export async function sendRegistration(firstName: string, lastName: string, dateOfBirth: Date, medicareNumber: string, reason: string) {
    console.log(firstName, lastName, dateOfBirth, medicareNumber, reason);
}